#include <stdio.h>
int main()
{int a=10;
float b = (float)54/5;
printf("value of b is : %f\n",b);
printf("value of a is : %d\n",a);
return 0;
}
// type casting is used to convert one data type into another data type. 
// In this example, integer and interer divison always gives integer value but if we want to get float value then we have to type cast one of the variable into float. In this example we have type casted 54 into float so the result will be float value.
